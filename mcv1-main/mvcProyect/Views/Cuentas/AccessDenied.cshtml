@{
    ViewData["Title"] = "Acceso denegado";
}

<h2>Acceso denegado</h2>
<p>No tienes permisos para acceder a esta página.</p>

@*  *@
<div>
    <p>Para configurar la base de datos, ejecute los siguientes comandos en la consola del paquete:</p>
    <code>dotnet ef migrations add Inicial</code>
    <code>dotnet ef database update</code>
</div>